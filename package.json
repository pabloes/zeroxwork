{
  "name": "ohmyserver",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "./node_modules/typescript/bin/tsc",
    "start": "echo 'node -> version '$(node --version) && cd frontend && npm install && npm run build && cd ../backend && npm install && npm run prod"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "typescript": "^5.6.2"
  }
}
